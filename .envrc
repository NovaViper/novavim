#!/usr/bin/env bash
# The shebang is there for editor formatting

watch_file flake.lock

watch_file default.nix
watch_file nix/*.nix
watch_file nix/**/*.nix

watch_file init.lua

# For lorri
watch_file .git/HEAD

use flake
